export default async function MyGuild({ params }) {
  // const {guild} = await params;
  return (
    <>
      {/* persistent my-guild details component [guild] as prop*/}
      {/* menu with links to each page in the below tree e.g. href=`guild-hall/${guild}/arena` */}
    </>
  );
}
